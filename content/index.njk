---
layout: layouts/base.njk
eleventyNavigation:
  key: "Home"
  order: 1
---

<style>{% include "css/message-box.css" %}</style>
<style>
.hero-container-simple {
    margin: 2rem auto;
    max-width: 50em;
    padding: 0 1rem;
}

.hero-container-simple .message-box {
    background-color: rgba(255, 255, 255, 0.9);
    padding: 3rem 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    text-align: center;
}

[data-theme="dark"] .hero-container-simple .message-box {
    background-color: rgba(21, 32, 43, 0.9);
}

.hero-container-simple h1 {
    margin-top: 0;
    font-size: 2.5rem;
    margin-bottom: 2rem;
}

.hero-container-simple p {
    font-size: 1.1rem;
    line-height: 1.6;
    margin: 1rem 0;
}
</style>
<div class="hero-container-simple">
  <div class="message-box">
    <h1>🌱 Welcome to my digital garden 🌱 </h1>

    <img src="../public/img/pre_surf.jpg" alt="Baby Tom" style="max-width: 350px; display: block; margin: 2rem auto; border-radius: 12px;" />

    <p>This is a space for me to dump all of the noise that is pulling me in all directions.</p>

    <p>If you're interested in what's going on in my life, follow along</p>
  </div>
</div>

{% set postsCount = collections.posts | length %}
{% set latestPostsCount = postsCount | min(3) %}
<h1>Latest {{ latestPostsCount }} Post{% if latestPostsCount != 1 %}s{% endif %}</h1>

{% set postslist = collections.posts | head(-1 * latestPostsCount) %}
{% set postslistCounter = postsCount %}
{% include "postslist.njk" %}

{# List every content page in the project #}
{#
<ul>
  {%- for entry in collections.all %}
  <li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
  {%- endfor %}
</ul>
#}
